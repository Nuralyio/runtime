version: '3'
services:
  veeapp-frontends:
    build:
      context: .
    ports:
      - "3000:3000"
    container_name: veeapp-frontends
    stdin_open: true
    volumes:
       - .:/usr/src/app
       - /usr/src/app/node_modules
    restart: unless-stopped
 
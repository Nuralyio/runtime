{
  "videoFields": {
    "container": {
      "uuid": "video_collapse_container",
      "name": "Video Fields Container",
      "style": {}
    },
    "collapse": {
      "uuid": "video_collapse",
      "title": "Video Properties",
      "style": {
        "--hy-collapse-content-small-size-padding": "5px",
        "--hy-collapse-font-weight": "normal",
        "--hy-collapse-border-radius": "0px",
        "--hy-collapse-width": "292px",
        "--hy-collapse-border": "none",
        "--hy-collapse-border-bottom": "1px solid #ccc",
        "--hy-collapse-local-header-background-color": "#3d3d3d"
      }
    },
    "properties": [
      {
        "name": "src",
        "label": "Src",
        "type": "text",
        "default": "",
        "width": "180px",
        "placeholder": "Video source URL",
        "valueHandler": "const selectedComponent = Utils.first(Vars.selectedComponents); if(selectedComponent.input?.src?.type=='value'){ return selectedComponent.input?.src?.value??''; } return '';",
        "stateHandler": "const selectedComponent = Utils.first(Vars.selectedComponents); let state = 'unabled'; if(selectedComponent.input?.src?.type =='handler'&&selectedComponent.input?.src?.value){ state = 'disabled' } return state;",
        "eventHandlers": {
          "valueChange": "const selectedComponent = Utils.first(Vars.selectedComponents); const newSrcText = EventData.value; updateInput(selectedComponent,'src','string',newSrcText);"
        },
        "hasHandler": true,
        "handlerType": "input",
        "handlerProperty": "src"
      },
      {
        "name": "previewable",
        "label": "Previewable",
        "type": "text",
        "default": "",
        "width": "180px",
        "placeholder": "Previewable",
        "valueHandler": "const selectedComponent = Utils.first(Vars.selectedComponents); if(selectedComponent.input?.previewable?.type=='value'){ return selectedComponent.input?.previewable?.value??''; } return '';",
        "stateHandler": "const selectedComponent = Utils.first(Vars.selectedComponents); let state = 'unabled'; if(selectedComponent.input?.previewable?.type =='handler'&&selectedComponent.input?.previewable?.value){ state = 'disabled' } return state;",
        "eventHandlers": {
          "valueChange": "const selectedComponent = Utils.first(Vars.selectedComponents); const newAltText = EventData.value; updateInput(selectedComponent,'previewable','string',newAltText);"
        },
        "hasHandler": true,
        "handlerType": "input",
        "handlerProperty": "previewable"
      },
      {
        "name": "poster",
        "label": "Poster",
        "type": "text",
        "default": "",
        "width": "180px",
        "placeholder": "Poster image URL",
        "valueHandler": "const selectedComponent = Utils.first(Vars.selectedComponents); if(selectedComponent.input?.poster?.type=='value'){ return selectedComponent.input?.poster?.value??''; } return '';",
        "stateHandler": "const selectedComponent = Utils.first(Vars.selectedComponents); let state = 'enabled'; if(selectedComponent.input?.poster?.type =='handler'&&selectedComponent.input?.poster?.value){ state = 'disabled' } return state;",
        "eventHandlers": {
          "valueChange": "const selectedComponent = Utils.first(Vars.selectedComponents); const newPoster = EventData.value; updateInput(selectedComponent,'poster','string',newPoster);"
        },
        "hasHandler": true,
        "handlerType": "input",
        "handlerProperty": "poster"
      },
      {
        "name": "preload",
        "label": "Preload",
        "type": "dropdown",
        "default": "metadata",
        "width": "180px",
        "placeholder": "Select preload mode",
        "options": [
          { "label": "None", "value": "none" },
          { "label": "Metadata", "value": "metadata" },
          { "label": "Auto", "value": "auto" }
        ],
        "valueHandler": "const selectedComponent = Utils.first(Vars.selectedComponents); if(selectedComponent.input?.preload?.type=='value'){ return selectedComponent.input?.preload?.value??'metadata'; } return 'metadata';",
        "stateHandler": "const selectedComponent = Utils.first(Vars.selectedComponents); let state = 'enabled'; if(selectedComponent.input?.preload?.type =='handler'&&selectedComponent.input?.preload?.value){ state = 'disabled' } return state;",
        "eventHandlers": {
          "valueChange": "const selectedComponent = Utils.first(Vars.selectedComponents); const newPreload = EventData.value; updateInput(selectedComponent,'preload','string',newPreload);"
        },
        "hasHandler": true,
        "handlerType": "input",
        "handlerProperty": "preload"
      }
    ]
  }
}

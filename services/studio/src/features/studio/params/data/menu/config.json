{
  "menuFields": {
    "container": {
      "uuid": "menu_collapse_container",
      "name": "Menu Fields Container",
      "style": {}
    },
    "collapse": {
      "uuid": "menu_collapse",
      "title": "Menu Properties"
    },
    "properties": [
      {
        "name": "items",
        "label": "Items",
        "type": "event",
        "width": "50px",
        "handlerType": "input",
        "handlerProperty": "options",
        "handlerValueGetter": "const parameter = 'options';\nlet labelHandler = '';\nconst selectedComponent = Utils.first($selectedComponents);\nif (selectedComponent?.input?.options?.type === 'handler' && selectedComponent?.input?.options?.value) {\n    labelHandler = selectedComponent?.input?.options.value;\n}else if (selectedComponent?.input?.options?.type ===  \"array\" && selectedComponent?.input?.options?.value) {\n    labelHandler = JSON.stringify(selectedComponent?.input?.options.value, null, 2);\n}\nreturn [parameter, labelHandler];",
        "handlerEventUpdate": "const selectedComponent = Utils.first($selectedComponents);\nif (EventData.value !== selectedComponent?.input?.options?.value) {\n    const isNotHandler = !EventData.value.includes('return');\n\n    if (isNotHandler) {\n        updateInput(\n            selectedComponent,\n            'options',\n            selectedComponent?.input?.options?.type,\n            JSON.parse(EventData.value)\n        );\n    } else {\n        updateInput(\n            selectedComponent,\n            'items',\n            'handler',\n            EventData.value\n        );\n    }\n}"
      },
      {
        "name": "menusize",
        "inputProperty": "size",
        "label": "Size",
        "type": "radio",
        "default": "medium",
        "width": "180px",
        "options": [
          { "label": "Small", "value": "small" },
          { "label": "Medium", "value": "medium" },
          { "label": "Large", "value": "large" }
        ],
        "stateHandler": { "ref": "inputHandler", "params": ["size"] },
        "hasHandler": true,
        "handlerType": "input",
        "handlerProperty": "size"
      },
      {
        "name": "menuarrowPosition",
        "inputProperty": "arrowPosition",
        "label": "Arrow Position",
        "type": "radio",
        "default": "right",
        "width": "180px",
        "options": [
          { "label": "Left", "value": "left" },
          { "label": "Right", "value": "right" }
        ],
        "stateHandler": { "ref": "inputHandler", "params": ["arrowPosition"] },
        "eventHandlers": {
          "onChange": { "ref": "updateInput", "params": ["arrowPosition", "value"] }
        },
        "hasHandler": true,
        "handlerType": "input",
        "handlerProperty": "arrowPosition"
      }
    ]
  }
}

---
import MakeLayout from "./layouts/MakeLayout.astro";

import "./app/studio/components/MainPanel/EditorInteractivePanel";
import "../shared/blocks/components/Tabs/Tabs";
import "../shared/blocks/components/Menu/Menu";
import "../shared/blocks/components/ColorPicker/colorpicker";
import "../shared/blocks/components/NumberInput/NumberInput";
import "../shared/blocks/wrappers/GenerikWrapper/GenerikWrapper";
import "../shared/blocks/components/IconButton/iconbutton";
import "../shared/blocks/components/Select/Select";
import { TabsPanel } from "./app/studio/components/MainPanel/TabsPanel";
import { TopbarPanelWrapper } from "./app/studio/components/TopBar/TopbarWrapper/TopbarWrapper";
import { LeftPanel } from "./app/studio/components/Layout/LeftPanel";
import { RightPanel } from "./app/studio/components/Layout/RightPanel";
import { $pages } from "../store/page"
import { $components } from "$store/component/component-sotre";
import { $applications } from "$store/apps";
import { registerContextInWebWorker } from "../utils/context-registration";
import { processHeaders } from "utils/api-calls-utils";
import { fetchAllApplications } from "$services/applications.service";
import { fetchApplicationPagesById } from "$services/page.service";
import { fetchApplicationComponentById } from "$services/component.service";
import { EditorSpinner } from './app/studio/components/Layout/EditorSpinner'

// TODO: handle error
const { app } = Astro.props;
const processedHeaders = processHeaders(Astro.request.headers);

let pageData, componentData, applications, application;

try {
  
  // Load application pages, components, and applications in parallel
  const [pageRequest, componentResponse, applicationsRequest] = await Promise.all([
    fetchApplicationPagesById(processedHeaders, app.uuid),
    fetchApplicationComponentById(processedHeaders, app.uuid),
    fetchAllApplications(processedHeaders)
  ]);

  // Handle page data
  pageData = pageRequest.data;
  $pages.set(pageData);

  // Handle component data
  componentData = componentResponse.data.map((component) => component.component);
  $components.set({ [app.uuid]: componentData });

  // Handle applications data
  applications = applicationsRequest.data;
  $applications.set(applications);
  application = applications.find((application) => application.uuid === app.uuid);

  // Further processing if needed
  registerContextInWebWorker({});
} catch (error) {
  // Handle errors here
  console.error("An error occurred while loading data:", error);
}
---

<head> <title>Nuraly - Editing {app.name}</title> </head>

<MakeLayout title="Make Apps">
  <div class="H-full flex flex-wrap mx-auto" style=" 
">
    <TopbarPanelWrapper client:load>
      <div
        class="border border-blue-300 shadow rounded-md p-4 m-4"
        style="width :98vw; margin-top: 4%;"
      >
        <div class="animate-pulse flex space-x-4">
          <div class="flex-1 space-y-6 py-1">
            <div class="space-y-3">
              <div class="grid grid-cols-3 gap-4">
                <div class="h-2 bg-slate-500 rounded col-span-2"></div>
                <div class="h-2 bg-slate-500 rounded col-span-1"></div>
              </div>
              <div class="h-2 bg-slate-500 rounded"></div>
            </div>
          </div>
        </div>
      </div>
      <div
        class="border border-blue-300 shadow rounded-md p-4 m-4"
        style="width :98vw; "
      >
        <div class="animate-pulse flex space-x-4">
          <div class="flex-1 space-y-6 py-1">
            <div class="space-y-3">
              <div class="grid grid-cols-3 gap-4">
                <div class="h-2 bg-slate-500 rounded col-span-2"></div>
                <div class="h-2 bg-slate-500 rounded col-span-1"></div>
              </div>
              <div class="h-2 bg-slate-500 rounded"></div>
            </div>
          </div>
        </div>
      </div>
      <div
        class="border border-blue-300 shadow rounded-md p-4 m-4"
        style="width :98vw; "
      >
        <div class="animate-pulse flex space-x-4">
          <div class="flex-1 space-y-6 py-1">
            <div class="space-y-3">
              <div class="grid grid-cols-3 gap-4">
                <div class="h-2 bg-slate-500 rounded col-span-2"></div>
                <div class="h-2 bg-slate-500 rounded col-span-1"></div>
              </div>
              <div class="h-2 bg-slate-500 rounded"></div>
            </div>
          </div>
        </div>
      </div>
    </TopbarPanelWrapper>
  </div>
  <div class="flex flex-row text-gray-800">
    <LeftPanel client:load />
    <main
      class="main flex flex-grow flex-col transition-all duration-150 ease-in md:ml-0"
    >
    <TabsPanel client:only="lit" />  
    <EditorSpinner client:load data=`${applications}`/>
    </main>
    <RightPanel client:only="lit" />
  </div>
</MakeLayout>
<script define:vars={{ pageData, componentData, applications, application }}>
  window.__INITIAL_PAGE_STATE__ = JSON.stringify(pageData);
  window.__INITIAL_COMPONENT_STATE__ = JSON.stringify(componentData);
  window.__INITIAL_APPLICATION_STATE__ = JSON.stringify(applications);
  window.__INITIAL_CURRENT_APPLICATION_STATE__ = JSON.stringify(application);
</script>
<style>
  html {
    overflow: hidden;
  }
  @media (prefers-color-scheme: dark) {
      body {
        --hybrid-tabs-content-background-color: #313131;
      background: #313131;

      height: 100vh;

      }
    }
    /* Spinner CSS */


</style>

---
import Layout from './_layouts/LandingLayout.astro';
import "../core/micro-app";
import { MicroApp } from '../core/micro-app';
const homeAppUUID = import.meta.env.HOME_APP_UUID;
const homeAppPageUUID = import.meta.env.HOME_PAGE_UUID;

---
<Layout title="Welcome to Astrolus.">

  <MicroApp
  client:idle
  mode="preview"
  uuid={homeAppUUID}
  page_uuid={homeAppPageUUID}
 ></MicroApp>
</Layout>
<style>
  html {
    overflow: hidden;
  }
  @media (prefers-color-scheme: dark) {
      html {
        --hybrid-tabs-content-background-color: #313131;
      background: #313131;

        color:#f8fafc;
      }
    }
</style>
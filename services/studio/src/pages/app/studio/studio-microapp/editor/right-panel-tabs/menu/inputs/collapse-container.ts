import { generateDynamicContainer } from "../../../utils/input-collapse-container-generator.ts";

export const StudioMenuCollapseContainer = generateDynamicContainer("menu_collapse_container", [
    "menu_data"
]); 
import { css } from "lit";

export default css`
   .quick-action{
    display: inline-flex;
    align-items: center;
    padding: 3px;
    position: absolute;
    margin-top: -55px;
    z-index: 10;
    border-radius: 5px;
    background: rgb(252 252 252);
    box-shadow: 0 0 5px 0 rgb(0 0 0 / 20%);
  }
`;

import { css } from "lit";

const appInfoStyle = css`
  .application-title {
    color: white;
    height: 60px;
    display: flex;
    align-items: center;
    user-select: none;
  }
`;

export default [appInfoStyle];

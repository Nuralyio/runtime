import { css } from "lit";

const UserInfoStyle = css`
  .user-info-initiator {
    height: 60px;
    display: flex;
    align-items: center;
    user-select: none;
    margin: 0 10px 0 10px;
  }
`;

export default [UserInfoStyle];

package com.nuraly.canvas.entity;

/**
 * Discriminator for different canvas types in the system.
 */
public enum CanvasType {
    WORKFLOW,    // Executable workflow canvas
    WHITEBOARD   // Collaborative whiteboard canvas
}

# Database Configuration
quarkus.datasource.db-kind=postgresql
quarkus.datasource.username=postgres
quarkus.datasource.password=password
quarkus.datasource.jdbc.url=jdbc:postgresql://postgres:5432/docgen_dev

# Hibernate - schema managed by Flyway
quarkus.hibernate-orm.database.generation=none

# Flyway Configuration
quarkus.flyway.migrate-at-start=false
quarkus.flyway.baseline-on-migrate=true
quarkus.flyway.baseline-version=0
quarkus.flyway.locations=classpath:db/migration
quarkus.flyway.clean-disabled=true

# Dev only - auto-migrate for convenience
%dev.quarkus.flyway.migrate-at-start=true

# Server Configuration
quarkus.http.port=7008
quarkus.http.cors.origins=*
quarkus.http.cors=true
quarkus.http.cors.methods=GET,POST,PUT,DELETE,OPTIONS

# RabbitMQ Configuration
rabbitmq.host=rabbitmq
rabbitmq.port=5672
rabbitmq.username=guest
rabbitmq.password=guest
rabbitmq.queue.generate=document.generate
rabbitmq.queue.ready=document.ready

# Upload Configuration
docgen.upload.path=/app/uploads

# Permission Configuration
permissions.api.url=http://api:8000/api/permissions/has
permission.api.base-url=http://api:8000/api
%dev.permissions.enabled=false
permissions.enabled=false

# OpenAPI/Swagger
quarkus.swagger-ui.always-include=true
quarkus.swagger-ui.path=/api/v1/documents/swagger-ui
quarkus.smallrye-openapi.info-title=Document Generator API
quarkus.smallrye-openapi.info-version=1.0.0
quarkus.smallrye-openapi.info-description=API for generating Word documents from templates

# Index shared library for CDI
quarkus.index-dependency.permission-lib.group-id=com.github.Nuralyio
quarkus.index-dependency.permission-lib.artifact-id=shared-java-library

# Journal Logging Configuration (via RabbitMQ)
journal.service.name=document-generator
journal.enabled=true
journal.async=true

# Logging
quarkus.log.level=INFO
quarkus.log.category."com.nuraly.docgen".level=DEBUG

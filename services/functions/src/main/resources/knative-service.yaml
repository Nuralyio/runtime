apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: ${functionName}  # Placeholder for function name
  namespace: default
spec:
  template:
    spec:
      containers:
        - image: ${image}:latest  # Placeholder for image name
          imagePullPolicy: Never  # Use local images from minikube Docker daemon
          env:
            - name: TARGET
              value: "lol"
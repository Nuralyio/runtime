# Health check endpoints - no authentication required

location = /health {
    return 200 '{"status":"UP"}';
    add_header Content-Type application/json;
}

location = /api/v1/functions/health {
    set $target $upstream_functions_api/q/health;
    proxy_pass $target;
    proxy_connect_timeout 3s;
    proxy_read_timeout 3s;
}

location = /api/v1/workflows/health {
    set $target $upstream_workflows_api/q/health;
    proxy_pass $target;
    proxy_connect_timeout 3s;
    proxy_read_timeout 3s;
}

location = /api/v1/kv/health {
    set $target $upstream_kv_api/q/health;
    proxy_pass $target;
    proxy_connect_timeout 3s;
    proxy_read_timeout 3s;
}

location = /api/v1/db/health {
    set $target $upstream_conduit_api/q/health;
    proxy_pass $target;
    proxy_connect_timeout 3s;
    proxy_read_timeout 3s;
}

location = /api/v1/logs/health {
    set $target $upstream_journal_api/q/health;
    proxy_pass $target;
    proxy_connect_timeout 3s;
    proxy_read_timeout 3s;
}

location = /api/v1/ocr/health {
    set $target $upstream_textlens_api/health;
    proxy_pass $target;
    proxy_connect_timeout 3s;
    proxy_read_timeout 3s;
}

location = /api/v1/crawler/health {
    set $target $upstream_parcour_api/q/health;
    proxy_pass $target;
    proxy_connect_timeout 3s;
    proxy_read_timeout 3s;
}

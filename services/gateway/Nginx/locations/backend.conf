
# WebSocket endpoint for real-time presence
location /socket.io/presence {
    access_by_lua_block {
        require("main").authenticateWithKeycloakPass(nil)
    }

    set $target $upstream_services_api;
    proxy_pass $target;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-USER $http_x_user;
    proxy_read_timeout 86400s;
    proxy_send_timeout 86400s;
}

# WebSocket endpoint for workflow execution events
location /socket.io/workflow {
    access_by_lua_block {
        require("main").authenticateWithKeycloakPass(nil)
    }

    set $target $upstream_services_api;
    proxy_pass $target;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-USER $http_x_user;
    proxy_read_timeout 86400s;
    proxy_send_timeout 86400s;
}

# Public endpoint for checking anonymous access (no auth required)
location ~ ^/api/resources/[^/]+/[^/]+/check-anonymous$ {
    set $target $upstream_services_api;
    proxy_pass $target;
    proxy_redirect off;
    proxy_cache_bypass $http_upgrade;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection keep-alive;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Host $server_name;
}

# Pages API with optional anonymous access (for preview mode)
location ~ ^/api/pages/application/([^/]+)$ {
    set $app_id $1;

    access_by_lua_block {
        require("main").authenticateWithOptionalAnonymous("application", ngx.var.app_id, ngx.var.app_id)
    }

    set $target $upstream_services_api;
    proxy_pass $target;
    proxy_redirect off;
    proxy_cache_bypass $http_upgrade;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection keep-alive;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Host $server_name;
    proxy_set_header X-USER $http_x_user;
}

# Components API with optional anonymous access (for preview mode)
location ~ ^/api/components/application/([^/]+)$ {
    set $app_id $1;

    access_by_lua_block {
        require("main").authenticateWithOptionalAnonymous("application", ngx.var.app_id, ngx.var.app_id)
    }

    set $target $upstream_services_api;
    proxy_pass $target;
    proxy_redirect off;
    proxy_cache_bypass $http_upgrade;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection keep-alive;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Host $server_name;
    proxy_set_header X-USER $http_x_user;
}

location /api/v1/functions {
    access_by_lua_block {
        require("main").authenticateWithKeycloakPass(nil)

    }
    set $target $upstream_functions_api/api/v1/functions;
    proxy_pass $target;
    proxy_redirect off;
    proxy_cache_bypass $http_upgrade;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection keep-alive;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Host $server_name;
}

location /api/v1/workflows {
    set $x_user "";
    access_by_lua_block {
        require("main").authenticateWithKeycloakPass(nil)
        -- Read X-USER header set by authenticateWithKeycloakPass
        -- Note: $http_x_user doesn't work because it reads from original client headers
        ngx.var.x_user = ngx.req.get_headers()["X-USER"] or ""
    }
    # Use proxy_pass without URI rewrite to preserve full path (e.g., /api/v1/workflows/{id})
    set $target $upstream_workflows_api;
    proxy_pass $target;
    proxy_http_version 1.1;
    proxy_redirect off;
    proxy_cache_bypass $http_upgrade;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Host $server_name;
    proxy_set_header X-USER $x_user;
}

location /api/v1/kv {
    access_by_lua_block {
        require("main").authenticateWithKeycloakPass(nil)
    }
    # Use proxy_pass without URI to preserve URL encoding (e.g., %2F for slashes in key paths)
    set $target $upstream_kv_api;
    proxy_pass $target;
    proxy_redirect off;
    proxy_cache_bypass $http_upgrade;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection keep-alive;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Host $server_name;
    proxy_set_header X-USER $http_x_user;
}

location /api/v1/db {
    access_by_lua_block {
        require("main").authenticateWithKeycloakPass(nil)
    }
    # Use proxy_pass without URI to preserve URL encoding
    set $target $upstream_conduit_api;
    proxy_pass $target;
    proxy_redirect off;
    proxy_cache_bypass $http_upgrade;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection keep-alive;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Host $server_name;
    proxy_set_header X-USER $http_x_user;
}

location /api/v1/ocr {
    access_by_lua_block {
        require("main").authenticateWithKeycloakPass(nil)
    }
    set $target $upstream_textlens_api;
    proxy_pass $target;
    proxy_redirect off;
    proxy_cache_bypass $http_upgrade;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection keep-alive;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Host $server_name;
    proxy_set_header X-USER $http_x_user;
    client_max_body_size 50M;
}

location /api/v1/logs {
    access_by_lua_block {
        require("main").authenticateWithKeycloakPass(nil)
    }
    set $target $upstream_journal_api;
    proxy_pass $target;
    proxy_redirect off;
    proxy_cache_bypass $http_upgrade;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection keep-alive;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Host $server_name;
    proxy_set_header X-USER $http_x_user;
}

# WebSocket endpoint for real-time log streaming
location /api/v1/logs/stream {
    access_by_lua_block {
        require("main").authenticateWithKeycloakPass(nil)
    }
    set $target $upstream_journal_api;
    proxy_pass $target;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-USER $http_x_user;
    proxy_read_timeout 86400s;
    proxy_send_timeout 86400s;
}

location /api {
    access_by_lua_block {
        require("main").authenticateWithKeycloakPass(nil)

    }
    set $target $upstream_services_api;
    proxy_pass $target;
    proxy_redirect off;
    proxy_cache_bypass $http_upgrade;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection keep-alive;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Host $server_name;
    proxy_set_header X-USER $http_x_user;
}

location ~* ^/(api/chat|api/chats) {
    set $chat_hostname '';

    access_by_lua_block {
        local chat_hostname = os.getenv("CHAT_HOSTNAME")
        ngx.log(ngx.INFO, "Chat hostname: ", chat_hostname)
        if chat_hostname then
            ngx.var.chat_hostname = chat_hostname
        else
            ngx.var.chat_hostname = "default_chat_hostname"  -- Provide a default value
            ngx.log(ngx.ERR, "CHAT_HOSTNAME environment variable is not set. Using default value.")
        end
        -- Log chat hostname
        ngx.log(ngx.INFO, "Chat hostname: ", ngx.var.chat_hostname)
    }

    access_log /var/log/openresty-chat.log;
    error_log /var/log/openresty-chat_error.log;

    proxy_pass http://$chat_hostname;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    client_max_body_size 0;

    # Additional security headers (optional)
    add_header X-Content-Type-Options nosniff;
    add_header X-XSS-Protection "1; mode=block";
}


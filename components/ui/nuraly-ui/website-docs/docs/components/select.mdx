---
sidebar_position: 11
---

# Select

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import "@nuralyui/select";
import { HySelect } from "@nuralyui/select/react";
import { useState } from "react";

export const BasicSelectDemo = () => {
  const [selectedOptions, setSelectedOptions] = useState([]);
  
  const options = [
    { value: 'apple', label: '🍎 Apple' },
    { value: 'banana', label: '🍌 Banana' },
    { value: 'orange', label: '🍊 Orange' },
    { value: 'grape', label: '🍇 Grape' }
  ];
  
  const handleChange = (e) => {
    setSelectedOptions(e.detail.value);
  };
  
  return (
    <div style={{ marginBottom: "20px" }}>
      <HySelect 
        placeholder="Choose an option"
        options={options}
        onChanged={handleChange}
        style={{ width: "200px" }}
      />
      {selectedOptions.length > 0 && (
        <div style={{ marginTop: "10px", color: "#666" }}>
          Selected: {selectedOptions.join(", ")}
        </div>
      )}
    </div>
  );
};

export const MultiSelectDemo = () => {
  const [selectedValues, setSelectedValues] = useState([]);
  
  const options = [
    { value: 'red', label: '🔴 Red' },
    { value: 'green', label: '🟢 Green' },
    { value: 'blue', label: '🔵 Blue' },
    { value: 'yellow', label: '🟡 Yellow' },
    { value: 'purple', label: '🟣 Purple' }
  ];
  
  const handleChange = (e) => {
    setSelectedValues(e.detail.value);
  };
  
  return (
    <div style={{ marginBottom: "20px" }}>
      <HySelect 
        selectionMode="multiple"
        placeholder="Select multiple options"
        options={options}
        onChanged={handleChange}
        style={{ width: "200px" }}
      />
      {selectedValues.length > 0 && (
        <div style={{ marginTop: "10px", color: "#666" }}>
          Selected: {selectedValues.join(", ")}
        </div>
      )}
    </div>
  );
};

export const SizeSelectDemo = () => {
  const options = [
    { value: '1', label: 'Option 1' },
    { value: '2', label: 'Option 2' },
    { value: '3', label: 'Option 3' }
  ];
  return (
    <div style={{ display: "flex", gap: "20px", flexDirection: "column" }}>
      <div>
        <label style={{ display: "block", marginBottom: "5px", fontSize: "14px" }}>Small Size:</label>
        <HySelect size="small" placeholder="Small select" options={options} style={{ width: "150px" }} />
      </div>
      <div>
        <label style={{ display: "block", marginBottom: "5px", fontSize: "14px" }}>Medium Size (Default):</label>
        <HySelect placeholder="Medium select" options={options} style={{ width: "180px" }} />
      </div>
      <div>
        <label style={{ display: "block", marginBottom: "5px", fontSize: "14px" }}>Large Size:</label>
        <HySelect size="large" placeholder="Large select" options={options} style={{ width: "220px" }} />
      </div>
    </div>
  );
};

export const DisabledSelectDemo = () => {
  const options = [
    { value: '1', label: 'Option 1' },
    { value: '2', label: 'Option 2' }
  ];
  return (
    <div style={{ display: "flex", gap: "20px", flexDirection: "column" }}>
      <div>
        <label style={{ display: "block", marginBottom: "5px", fontSize: "14px" }}>Enabled Select:</label>
        <HySelect placeholder="This is enabled" options={options} style={{ width: "200px" }} />
      </div>
      <div>
        <label style={{ display: "block", marginBottom: "5px", fontSize: "14px" }}>Disabled Select:</label>
        <HySelect disabled placeholder="This is disabled" options={options} style={{ width: "200px" }} />
      </div>
    </div>
  );
};

The Select component allows users to choose one or multiple options from a dropdown list. It supports various configurations including single/multi-select, different sizes, and custom styling.

## How to use it

<Tabs>
  <TabItem value="npm" label="npm">

```shell
npm install @nuralyui/select
```

</TabItem>
 <TabItem value="yarn" label="yarn">

```shell
yarn add @nuralyui/select
```

</TabItem>
</Tabs>

## Import

<Tabs>
  <TabItem value="html" label="HTML">

```html
<script type="module">
  import '@nuralyui/select';
</script>

<script>
const options = [
  { value: 'apple', label: 'Apple' },
  { value: 'banana', label: 'Banana' },
  { value: 'orange', label: 'Orange' }
];

const selectElement = document.querySelector('hy-select');
selectElement.options = options;
selectElement.addEventListener('changed', (e) => {
  console.log('Selected:', e.detail.value);
});
</script>

<hy-select placeholder="Choose an option"></hy-select>
```

</TabItem>
  <TabItem value="react" label="React">

```jsx
import { HySelect } from '@nuralyui/select/react';

function App() {
  const options = [
    { value: 'apple', label: 'Apple' },
    { value: 'banana', label: 'Banana' },
    { value: 'orange', label: 'Orange' }
  ];

  const handleChange = (e) => {
    console.log('Selected:', e.detail.value);
  };

  return (
    <HySelect 
      placeholder="Choose an option"
      options={options}
      onChanged={handleChange}
    />
  );
}
```

</TabItem>
</Tabs>

## API Reference

### Properties

| Property | Type | Default | Description |
|----------|------|---------|-------------|
| `placeholder` | `string` | `undefined` | Placeholder text when no option is selected |
| `options` | `Array<{value: string, label: string}>` | `[]` | Array of option objects with value and label |
| `selectionMode` | `'single' \| 'multiple'` | `'single'` | Whether single or multiple selections are allowed |
| `defaultSelected` | `string[]` | `[]` | Array of default selected values |
| `disabled` | `boolean` | `false` | Whether the select is disabled |
| `size` | `'small' \| 'medium' \| 'large'` | `'medium'` | The size of the select |
| `status` | `'default' \| 'error' \| 'warning'` | `'default'` | The status/validation state |

### Events

| Event | Description |
|-------|-------------|
| `changed` | Fired when the selection changes, returns `e.detail.value` array |

## Examples

### Basic Select

Try selecting different options to see the component in action:

<BasicSelectDemo />

<details style={{backgroundColor : 'transparent', border : 'none'}}>
  <summary>Show code</summary>
<Tabs>
  <TabItem value="react" label="React">

```jsx
import { HySelect } from "@nuralyui/select/react";
import { useState } from "react";

export default function App() {
  const [selectedOptions, setSelectedOptions] = useState([]);
  
  const options = [
    { value: 'apple', label: '🍎 Apple' },
    { value: 'banana', label: '🍌 Banana' },
    { value: 'orange', label: '🍊 Orange' },
    { value: 'grape', label: '🍇 Grape' }
  ];
  
  const handleChange = (e) => {
    setSelectedOptions(e.detail.value);
  };
  
  return (
    <div>
      <HySelect 
        placeholder="Choose an option"
        options={options}
        onChanged={handleChange}
      />
      {selectedOptions.length > 0 && (
        <div>Selected: {selectedOptions.join(", ")}</div>
      )}
    </div>
  );
}
```

</TabItem>
<TabItem value="Html/Angular" label="Html/Angular">

```html
<script>
const options = [
  { value: 'apple', label: '🍎 Apple' },
  { value: 'banana', label: '🍌 Banana' },
  { value: 'orange', label: '🍊 Orange' },
  { value: 'grape', label: '🍇 Grape' }
];

const selectElement = document.querySelector('hy-select');
selectElement.options = options;
selectElement.addEventListener('changed', (e) => {
  console.log('Selected:', e.detail.value);
});
</script>

<hy-select placeholder="Choose an option"></hy-select>
```

</TabItem>
</Tabs>
</details>

### Multi-Select

Select multiple options by holding Ctrl (or Cmd on Mac):

<MultiSelectDemo />

<details style={{backgroundColor : 'transparent', border : 'none'}}>
  <summary>Show code</summary>
<Tabs>
  <TabItem value="react" label="React">

```jsx
import { HySelect } from "@nuralyui/select/react";
import { useState } from "react";

export default function App() {
  const [selectedValues, setSelectedValues] = useState([]);
  
  const handleChange = (e) => {
    const value = Array.from(e.target.selectedOptions, option => option.value);
    setSelectedValues(value);
  };
  
  return (
    <div>
      <HySelect 
        multiple 
        placeholder="Select multiple options"
        onChange={handleChange}
      >
        <option value="red">🔴 Red</option>
        <option value="green">🟢 Green</option>
        <option value="blue">🔵 Blue</option>
        <option value="yellow">🟡 Yellow</option>
        <option value="purple">🟣 Purple</option>
      </HySelect>
      {selectedValues.length > 0 && (
        <div>Selected: {selectedValues.join(", ")}</div>
      )}
    </div>
  );
}
```

</TabItem>
<TabItem value="Html/Angular" label="Html/Angular">

```html
<hy-select multiple placeholder="Select multiple options">
  <option value="red">🔴 Red</option>
  <option value="green">🟢 Green</option>
  <option value="blue">🔵 Blue</option>
  <option value="yellow">🟡 Yellow</option>
  <option value="purple">🟣 Purple</option>
</hy-select>
```

</TabItem>
</Tabs>
</details>

### Different Sizes

The select component supports different sizes for various use cases:

<SizeSelectDemo />

<details style={{backgroundColor : 'transparent', border : 'none'}}>
  <summary>Show code</summary>
<Tabs>
  <TabItem value="react" label="React">

```jsx
import { HySelect } from "@nuralyui/select/react";

export default function App() {
  return (
    <div>
      <div>
        <label>Small Size:</label>
        <HySelect size="small" placeholder="Small select">
          <option value="1">Option 1</option>
          <option value="2">Option 2</option>
          <option value="3">Option 3</option>
        </HySelect>
      </div>
      
      <div>
        <label>Medium Size (Default):</label>
        <HySelect placeholder="Medium select">
          <option value="1">Option 1</option>
          <option value="2">Option 2</option>
          <option value="3">Option 3</option>
        </HySelect>
      </div>
      
      <div>
        <label>Large Size:</label>
        <HySelect size="large" placeholder="Large select">
          <option value="1">Option 1</option>
          <option value="2">Option 2</option>
          <option value="3">Option 3</option>
        </HySelect>
      </div>
    </div>
  );
}
```

</TabItem>
<TabItem value="Html/Angular" label="Html/Angular">

```html
<hy-select size="small" placeholder="Small select">
  <option value="1">Option 1</option>
  <option value="2">Option 2</option>
</hy-select>

<hy-select placeholder="Medium select (default)">
  <option value="1">Option 1</option>
  <option value="2">Option 2</option>
</hy-select>

<hy-select size="large" placeholder="Large select">
  <option value="1">Option 1</option>
  <option value="2">Option 2</option>
</hy-select>
```

</TabItem>
</Tabs>
</details>

### Disabled State

Compare enabled vs disabled select components:

<DisabledSelectDemo />

<details style={{backgroundColor : 'transparent', border : 'none'}}>
  <summary>Show code</summary>
<Tabs>
  <TabItem value="react" label="React">

```jsx
import { HySelect } from "@nuralyui/select/react";

export default function App() {
  return (
    <div>
      <div>
        <label>Enabled Select:</label>
        <HySelect placeholder="This is enabled">
          <option value="1">Option 1</option>
          <option value="2">Option 2</option>
        </HySelect>
      </div>
      
      <div>
        <label>Disabled Select:</label>
        <HySelect disabled placeholder="This is disabled">
          <option value="1">Option 1</option>
          <option value="2">Option 2</option>
        </HySelect>
      </div>
    </div>
  );
}
```

</TabItem>
<TabItem value="Html/Angular" label="Html/Angular">

```html
<!-- Enabled -->
<hy-select placeholder="This is enabled">
  <option value="1">Option 1</option>
  <option value="2">Option 2</option>
</hy-select>

<!-- Disabled -->
<hy-select disabled placeholder="This is disabled">
  <option value="1">Option 1</option>
  <option value="2">Option 2</option>
</hy-select>
```

</TabItem>
</Tabs>
</details>
